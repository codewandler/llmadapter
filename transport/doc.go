// Package transport contains byte-stream transport primitives, HTTP execution,
// SSE and NDJSON readers, retry/rate-limit wrappers, fake transports for tests,
// and the default decompression-aware HTTP client.
package transport
